# 帳務分頁：介面 Wireframe 與切換動畫規格

## 設計目標

「帳務」將合併現有的**分析**與**明細**功能，取代兩個獨立的底部導覽項目。使用者可在同一頁上方，以明確的兩段式控制項切換兩種工作模式：先理解支出結構，再查詢或管理個別交易。此設計保留目前所有篩選、排序、月份切換與分類跳轉能力，並縮短底部導覽。

> 預設進入「分析」，因為它最適合作為帳務資料的概覽入口；若使用者由支出排行點選分類，則直接開啟「明細」並帶入該分類條件。

## 分頁導覽結構

底部導覽建議調整為四個主要入口，讓「帳務」承接分析與明細功能。

| 順序 | 底部導覽 | 角色 | 預設內容 |
|---:|---|---|---|
| 1 | 總覽 | 年度財務總覽 | 收入、支出、生活金額、存款目標、分類地圖 |
| 2 | 帳務 | 分析與交易查詢工作區 | 分析 |
| 3 | 新增 | 主要行動按鈕 | 新增收支交易 |
| 4 | 設定 | 匯入與目標設定 | 存款目標、Excel 匯入、清除記錄 |

## 主畫面 Wireframe

### 固定骨架

```text
┌─────────────────────────────────────┐
│ 帳務                                │
│ 查看分析或查詢個別交易紀錄           │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │              分析 │ 明細        │ │
│ └─────────────────────────────────┘ │
│                                     │
│              動態內容區              │
└─────────────────────────────────────┘
```

標題與分段切換控制項在兩種模式間保持固定。控制項高度為 **40px**，外框使用暖灰色邊線，選取狀態使用淺翡翠綠底色與深翡翠文字。這使切換時使用者仍保有明確的空間定位。

### 分析狀態

```text
┌─────────────────────────────────────┐
│ 帳務                                │
│ [        分析        ][    明細    ] │
│                                     │
│ [ 總年度 ] [ 2026 年度 ▾ ]          │
│                                     │
│ ┌─ 快速月份 ──────────────────────┐ │
│ │  1月  2月  3月  4月              │ │
│ │  5月  6月  7月  8月              │ │
│ │  9月 10月 11月 12月              │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─ 支出排行 ──────────────────────┐ │
│ │ #1 ● 帳戶轉帳          NT$ ... │ │
│ │ #2 ● 現金提款          NT$ ... │ │
│ │ #3 ● 購物              NT$ ... │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─ 月度排行趨勢 ──────────────────┐ │
│ │  ▂▃▅▂▆▃     最近六個月趨勢      │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

使用者點選排行列時，應自動切換至「明細」，並攜帶該分類作為篩選條件。月份控制維持目前的年度與指定月份邏輯。

### 明細狀態

```text
┌─────────────────────────────────────┐
│ 帳務                                │
│ [    分析    ][        明細        ] │
│                                     │
│ ┌─ 篩選與排序 ─────────────── 24筆 ┐│
│ │ 日期／金額範圍／排序       篩選 ▾ ││
│ └─────────────────────────────────┘ │
│                                     │
│ ○ 入  薪資                    +... │
│       7月30日週四                    │
│ ─────────────────────────────────── │
│ ○ 出  餐飲                    −... │
│       7月28日週二                    │
│ ─────────────────────────────────── │
│ ○ 出  購物                    −... │
│       7月26日週日                    │
└─────────────────────────────────────┘
```

明細模式預設收合篩選欄，只露出「篩選與排序」摘要、啟用條件數與符合筆數；點選後展開既有的日期區間、金額範圍、排序與清除功能。由分析切入時，分類條件需保留為可見的篩選標籤，例如「分類：購物 ×」。

## 切換動畫 Wireframe

```text
分析已選取                    切換中                      明細已選取
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│ [ 分析 ][明細]│       │ [分析 ][明細]│       │ [分析 ][ 明細]│
│  ▔▔▔▔       │       │     ▔▔▔▔     │       │        ▔▔▔▔  │
│  排行卡片    │  →    │ 排行  ⇢  明細 │  →    │    交易清單  │
│  不透明      │       │  50%   50%   │       │    不透明    │
└──────────────┘       └──────────────┘       └──────────────┘
```

| 動畫元素 | 規格 | 目的 |
|---|---|---|
| 分段指示器 | `translateX`，**180ms**，`ease-out` | 清楚標示目前模式，不造成跳動感 |
| 動態內容 | 離開內容 `translateX(-8px)`、淡出；進入內容 `translateX(8px → 0)`、淡入 | 建立同一工作區內的前後頁感受 |
| 內容透明度 | 0.0 → 1.0，**160ms** | 減少圖表與列表突然替換的視覺干擾 |
| 列表定位 | 明細模式保留上次捲動位置；切回分析亦保留選取年月 | 支援來回比較，不遺失工作脈絡 |
| 無障礙偏好 | 啟用減少動態效果時，只保留 **120ms** 淡入淡出 | 尊重系統動態效果設定 |

切換控制項應在按下後立即更新選取狀態；內容轉場在指示器起始移動後約 **20ms** 開始。若使用者連續快速點選，應以最新選取狀態為準，不排隊播放前一段動畫。

## 關鍵互動流程

| 起點 | 使用者操作 | 系統結果 |
|---|---|---|
| 分析 | 點選「明細」 | 切換至明細，保留既有明細的篩選與排序狀態 |
| 明細 | 點選「分析」 | 切換至分析，保留分析的年度與月份選擇 |
| 支出排行 | 點選分類列 | 切換至明細，帶入該分類篩選，篩選欄顯示分類標籤 |
| 明細 | 點選「篩選與排序」 | 展開日期、金額與排序控制項；不影響目前列表位置 |
| 分類明細 | 移除分類標籤 | 顯示完整交易清單，保留其他篩選條件 |

## 實作優先順序

第一步先建立單一「帳務」路由與頂端分段切換，並將現有分析／明細元件移為可嵌入內容。第二步使支出排行的分類跳轉改為切換到同頁明細狀態。最後再實作篩選欄收合與上述轉場動畫，以避免先加入動畫後再調整資訊架構。
